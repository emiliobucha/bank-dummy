pipeline {
    agent {label 'bc-sebastianmascolo'}
    stages {
        stage('Build') {
            steps {
                sh 'mvn package -B -DskipTests'
            }
        }
    }
}

